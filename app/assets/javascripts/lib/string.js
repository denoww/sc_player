// Generated by CoffeeScript 2.5.1
(function() {
  var charToAccentedCharClassMap;

  String.prototype.removerAcentos = function() {
    var k, result, v;
    result = this;
    for (k in charToAccentedCharClassMap) {
      v = charToAccentedCharClassMap[k];
      result = result.replace(new RegExp(v, "g"), k);
    }
    return result;
  };

  String.prototype.ljust = function(width, padding) {
    var resp;
    padding = padding || ' ';
    padding = padding.substr(0, 1);
    if (this.length < width) {
      resp = this + padding.repeat(width - this.length);
    } else {
      resp = this;
    }
    return resp.toString();
  };

  String.prototype.rjust = function(width, padding) {
    var resp;
    padding = padding || ' ';
    padding = padding.substr(0, 1);
    if (this.length < width) {
      resp = padding.repeat(width - this.length) + this;
    } else {
      resp = this;
    }
    return resp.toString();
  };

  String.prototype.toI = function() {
    return parseInt(this.replace(",", "."), 10);
  };

  String.prototype.toPascalCase = function() {
    var arr, i, l;
    arr = this.split(/\s|_/);
    i = 0;
    l = arr.length;
    while (i < l) {
      arr[i] = arr[i].substr(0, 1).toUpperCase() + (arr[i].length > 1 ? arr[i].substr(1).toLowerCase() : "");
      i++;
    }
    return arr.join('');
  };

  String.prototype.removeSpecialCharacters = function() {
    return this.replace(/[^\w\s]/gi, '');
  };

  charToAccentedCharClassMap = {
    A: "[AÁÀÃÂÄǍȀĀȦ]",
    a: "[aáàãâäǎȁāȧ]",
    E: "[EÉÈẼÊËĚȄĒĖ]",
    e: "[eéèẽêëěȅēė]",
    I: "[IÍÌĨÎÏǏȈĪİ]",
    i: "[iíìĩîïǐȉīi̇]",
    O: "[OÓÒÕÔÖǑȌŌȮ]",
    o: "[oóòõôöǒȍōȯ]",
    U: "[UÚÙŨÛÜǓȔŪ]",
    u: "[uúùũûüǔȕū]",
    C: "[CÇ]",
    c: "[cç]"
  };

}).call(this);
